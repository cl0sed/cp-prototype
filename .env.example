# PostgreSQL Settings (used by db service in docker-compose.yml)
POSTGRES_USER=user_placeholder
POSTGRES_PASSWORD=password_placeholder
POSTGRES_DB=db_name_placeholder

# These variables are just examples for reference
# DO NOT include actual passwords or sensitive data in this file
# Copy this file to .env and replace with actual values
# IMPORTANT: .env files should be added to .gitignore
