"""
Initialization for the main application package.

This file marks the `app` directory as a Python package.
It can potentially be used for package-level initializations or imports,
but is often kept empty.
"""
