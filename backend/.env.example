# AI Video Creation Platform - Environment Variables Example
# --------------------------------------------------------------
# This file contains example environment variables for the application.
# Copy this file to '.env' for local development and fill in appropriate values.
# DO NOT commit the actual .env file or any real secrets to version control!
#
# IMPORTANT: For staging/production environments, these values should be
# provided via environment variables injected securely (e.g., from a secrets manager),
# NOT from a .env file.

# Environment Identifier
# Can be "development", "staging", "production"
ENVIRONMENT=development

# PoC Required Variables
# ---------------------
# These variables are required for the application to function properly even in PoC stage

# PostgreSQL connection string (required)
# Example: postgresql://user:password@localhost:5432/appdb
DATABASE_URL=

# Redis connection string (required)
# Example: redis://localhost:6379/0
REDIS_URL=

# Logging level (optional, defaults to "INFO")
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# API key for LLM provider (optional for early PoC stages)
# This is a sensitive value and should be kept secure
# LLM_API_KEY=your_api_key_here


# MVP Optional Placeholders (Set Later)
# ------------------------------------
# These variables are not required for PoC but will be needed for MVP
# They are defined here to establish the structure for future expansion

# Sentry DSN for error tracking (optional for PoC, needed for MVP)
# SENTRY_DSN=https://your_sentry_dsn_here

# PostHog API key for analytics (optional for PoC, needed for MVP)
# This is a sensitive value and should be kept secure
# POSTHOG_API_KEY=your_posthog_api_key_here

# OpenTelemetry Exporter endpoint (optional for PoC, needed for MVP)
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your_otel_endpoint_here
