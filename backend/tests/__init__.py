"""
Initialization for the Tests package.

This file marks the `tests` directory as a Python package.
It allows test runners (like pytest) to discover tests within this directory
and its subdirectories.
Often kept empty.
"""
